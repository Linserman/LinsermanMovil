import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.linserman.app',
  appName: 'Linserman',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
